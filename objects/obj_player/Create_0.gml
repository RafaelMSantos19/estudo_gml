// declarando variavel global e numerica:

vidas =5;

// declarando uma variavel global e textual:

nomde_da_fase = "Fase da agua";

// declarando uma variavel do tipo booleano:

vivo = true;


// mostrando mensagem na tela:

show_message(vidas);
show_message(nomde_da_fase);
show_message(vivo);